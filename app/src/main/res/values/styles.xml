<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MyAlertDialogStyle" parent="AlertDialog.AppCompat">
        <item name="android:backgroundDimAmount">0.5</item>     //灰度
        <item name="android:windowFrame">@null</item>            //边框
        <item name="android:windowNoTitle">false</item>　　　　  //标题栏
        <item name="android:windowIsFloating">true</item>       //是否浮动在Activity之上
        <item name="android:windowIsTranslucent">true</item>     //是否半透明
        <item name="android:windowContentOverlay">@null</item>    //是否有覆盖
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>   //设置Activity出现形式
        <item name="android:backgroundDimEnabled">true</item>     //背景是否模糊显示
        <item name="android:background">@drawable/circlecorner</item>  //设置背景圆角
    </style>

</resources>
